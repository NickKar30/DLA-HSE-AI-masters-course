# Домашнее задание 3 (TTS): Vocoders

## Цель работы

Реализовать с один из вокодеров для синтеза речи на русском языке.

В качестве данных для обучения мы будем использовать датасет [RUSLAN](https://ruslan-corpus.github.io)

> [!IMPORTANT]
> * **Запрещено** использовать готовые предобученные модели вокодеров в качестве основы

**Важно:** поскольку ваша работа - это по сути переимплементация статьи, вы **должны** описать все шаги подробно. Например:
* Как организована архитектура модели
* Какие лосс-функции использованы
* Как вы обрабатываете mel-спектрограммы
* Как вы улучшаете базовую модель

---

## Доступные модели вокодеров

Выберите одну из следующих моделей для реализации:

### 1. HiFi-GAN
* **Статья:** [HiFi-GAN: Generative Adversarial Networks for Efficient and High Fidelity Speech Synthesis](https://arxiv.org/pdf/2010.05646.pdf)

### 2. iSTFTNet
* **Статья:** [iSTFTNet : Fast Neural Audio Vocoding with Synergy of ISTFT and Residual Blocks](https://arxiv.org/pdf/2203.02395.pdf)

### 3. Vocos
* **Статья:** [Vocos: Closing the gap between time-domain and Fourier-based neural vocoders](https://arxiv.org/pdf/2306.00814.pdf)

---

## Обязательные требования

Общий формат этого домашнего задания следует формату большого домашнего домашнего задания.
Вы должны организовать репозиторий как в домашнем задании по ASR - разбить код на модули и следовать стилю кода.

Мы не принимаем домашнее задание, если не выполнены следующие требования:

* Код должен находится в публичном репозитории GitHub (или GitLab)
* Все необходимые пакеты указаны в `./requirements.txt` или в разделе инструкций установки в `README.md`
* Вы должны использовать **W&B** или **CometML** для логирования потерь и синтезированного аудио
* Все необходимые ресурсы (чекпойнты модели) должны быть загружаемы скриптом
  - Упомяните скрипт в `README.md`
* Приложите отчет, который включает:
  * Описание выбранной модели и результаты каждого эксперимента
  * Как воспроизвести вашу модель?
  * Логи обучения (чтобы показать, как быстро обучалась сеть)
  * Что сработало, а что нет?
  * Какие были основные сложности?

---

## Оценка

**Максимальная оценка: 100 баллов + бонусы**

```
базовая_оценка =  MOS + (качество кода и отчета)
итоговая_оценка = базовая_оценка + бонусы
```

### Распределение баллов:

- **MOS (Mean Opinion Score): 50 баллов**

Проверяющие будут прослушивать синтезированное вашим вокодером аудио и сравнивать его с оригиналом, оценивая качество звука по следующей шкале:

**Шкала оценивания:**
* **50**: Аудио высокого качества, артефакты минимальны
* **40**: Есть небольшой шум, но аудио приятное и натуральное
* **30**: Заметные артефакты, но качество приемлемое
* **20**: Значительный шум и искажения, но речь понятна
* **10**: Очень плохое качество с сильными артефактами
* **0**: Непригодное к использованию

- **Код и отчет: 50 баллов**
  - Качество реализации (20 баллов)
  - Подробный отчет с описанием (15 баллов)
  - Логи обучения и воспроизводимость (10 баллов)
  - Структура проекта и документация (5 баллов)

### MOS (Mean Opinion Score) оценка

Вы должны синтезировать аудио из мел-спектрограмм, извлеченных из следующих предложений:

[TODO]

**Важно:** Для оценки вокодера используйте мел-спектрограммы, извлеченные из **ground-truth аудио** этих предложений. Это позволит оценить качество вокодера без влияния ошибок акустической модели. Соответствующие аудиофайлы будут предоставлены отдельно [TODO].

### Бонусы

* **(+5 баллов)** Ablation study: проведите эксперименты с различными компонентами модели (например, разные функции активации, количество discriminators, различные типы лоссов) и проанализируйте их влияние на качество синтеза. Предоставьте количественные и качественные сравнения результатов.
* **(+5 баллов)** Улучшение робастности модели к синтетическим мел-спектрограммам (сгенерированным акустической моделью text-to-mel). Объясните ваш подход и как он должен помочь. Сравните чекпойнты до и после улучшения робастности количественно (используя метрики PESQ, STOI, UTMOS, FAD, NISQA и т.д.) и качественно (анализируя сгенерированные образцы как в разделе Отчет). Ожидается детальный анализ с глубоким погружением.
* **(до +10 баллов)** Добавление архитектурных улучшений из современных работ (например, Snake activation из [BigVGAN](https://arxiv.org/pdf/2206.04658.pdf)). Каждое улучшение оценивается до 5 баллов. Обязательно обоснуйте выбор улучшений и продемонстрируйте их эффективность через сравнение с baseline моделью.
* **(+5 баллов)** Использование [DVC (Data Version Control)](https://dvc.org/) для управления датасетом и чекпойнтами модели

---

## Пайплайн работы

### Этап 1: Подготовка данных

Подготовьте датасет RUSLAN:
* Извлеките mel-спектрограммы из аудиофайлов
* Нормализуйте спектрограммы
* Разделите данные на train/test

**Важно:** Чтобы избежать несоответствия между обучающими и тестовыми признаками, вы должны использовать один и тот же код для получения мел-спектрограмм из аудио. Пример конфигурации приведен ниже (см. раздел Тестирование и при необходимости измените конфигурацию мел-спектрограммы).

> [!NOTE]
> Датасет RUSLAN записан с частотой дискретизации **44100 Hz**, а конфигурация ниже использует **22050 Hz**. Ресемплирование необязательно, но рекомендуется:
> - **22050 Hz**: стандарт для TTS, меньше вычислительных затрат
> - **44100 Hz**: потенциально лучше сохраняет высокие частоты, но требует изменения конфигурации (`sr=44100`, `f_max=22050`) и больше памяти/вычислений

<details>
<summary>Пример кода для извлечения мел-спектрограмм</summary>

```python
from dataclasses import dataclass

import torch
from torch import nn

import torchaudio
import librosa


@dataclass
class MelSpectrogramConfig:
    sr: int = 22050
    win_length: int = 1024
    hop_length: int = 256
    n_fft: int = 1024
    f_min: int = 0
    f_max: int = 11025
    n_mels: int = 80
    power: float = 2.0

    # value of melspectrograms if we fed a silence into `MelSpectrogram`
    pad_value: float = -11.5129251


class MelSpectrogram(nn.Module):

    def __init__(self, config: MelSpectrogramConfig):
        super(MelSpectrogram, self).__init__()

        self.config = config

        self.mel_spectrogram = torchaudio.transforms.MelSpectrogram(
            sample_rate=config.sr,
            win_length=config.win_length,
            hop_length=config.hop_length,
            n_fft=config.n_fft,
            f_min=config.f_min,
            f_max=config.f_max,
            n_mels=config.n_mels
        )

        # The is no way to set power in constructor in 0.5.0 version.
        self.mel_spectrogram.spectrogram.power = config.power

        # Default `torchaudio` mel basis uses HTK formula. In order to be compatible with WaveGlow
        # we decided to use Slaney one instead (as well as `librosa` does by default).
        mel_basis = librosa.filters.mel(
            sr=config.sr,
            n_fft=config.n_fft,
            n_mels=config.n_mels,
            fmin=config.f_min,
            fmax=config.f_max
        ).T
        self.mel_spectrogram.mel_scale.fb.copy_(torch.tensor(mel_basis))

    def forward(self, audio: torch.Tensor) -> torch.Tensor:
        """
        :param audio: Expected shape is [B, T]
        :return: Shape is [B, n_mels, T']
        """

        mel = self.mel_spectrogram(audio) \
            .clamp_(min=1e-5) \
            .log_()

        return mel
```

</details>

### Этап 2: Реализация и обучение модели

Реализуйте архитектуру выбранного вокодера

### Этап 3: Синтез аудио

* Загрузите обученный вокодер
* Синтезируйте аудио из мел-спектрограмм для тестовых предложений

---

## Тестирование

Вы должны добавить скрипт `synthesize.py` и класс датасета `CustomDirDataset` в `src/datasets/` с соответствующей конфигурацией в `src/configs/`.

### CustomDirDataset

`CustomDirDataset` должен уметь обрабатывать любую директорию следующего формата:

```
NameOfTheDirectoryWithUtterances
└── audio/
    ├── audio1.wav
    ├── audio2.wav
    .
    .
    .
    └── audioN.wav
```

Датасет должен:
- Извлекать мел-спектрограммы из аудиофайлов используя ту же конфигурацию, что использовалась при обучении
- Иметь аргумент для пути к директории, который можно изменить через Hydra-опции
- Поддерживать как обычный синтез (из мел-спектрограмм акустической модели), так и **resynthesize режим** (извлечение мел-спектрограмм из ground-truth аудио)

### Скрипт synthesize.py

Скрипт `synthesize.py` должен:
- Применять обученную модель к заданному датасету
- Сохранять сгенерированные аудиофайлы в указанную директорию
- Сохранять имена файлов так, чтобы их можно было сопоставить с исходными

**Обязательно укажите в README.md команды для запуска инференса на вашей финальной модели.**

> [!TIP]
> Для раздела "Отчет" будет полезно добавить опцию **resynthesize**, при которой ground-truth аудио используется для извлечения соответствующей мел-спектрограммы, которая затем преобразуется обратно в аудио вашим вокодером. Это позволяет оценить качество вокодера в изоляции, без влияния ошибок акустической модели.

---

## Демо

Для того, чтобы другие могли с легкостью пользоваться вашим решением и понимать как работает ваш код, хорошей практикой является создание демо (дополнительно к хорошим инструкциям в README), например, в виде IPython-ноутбука. **Вы должны приложить такой демо-ноутбук в ваш Git**.

Демо-ноутбук должен делать следующее:

1. Клонирует ваш репозиторий и выполняет шаги установки из `README`.
2. Скачивает все необходимые веса (вокодер, акустическая модель если нужна).
3. Показывает, как запускать `synthesize.py` на примере датасета.
4. Демонстрирует **три режима работы**:
   - **Inference на датасете**: обработка директории с аудиофайлами
   - **Resynthesize режим**: извлечение мел-спектрограмм из ground-truth аудио и их синтез обратно
   - **Full TTS pipeline** (опционально): генерация аудио из текста через акустическую модель + ваш вокодер
5. Синтезирует аудио для тестовых предложений из раздела MOS
6. Демонстрирует возможности вокодера (качество, скорость инференса).
7. Все это сопровождается комментариями и пояснениями.

> [!IMPORTANT]
> Вы **обязаны** проверить ваш инференс и убедиться, что он работает без проблем, иначе вы получите $0$ за домашнее задание. Это связано с тем, что ваш код должен быть рабочим и проверка его работоспособности -- задача разработчика, а не пользователей репозитория. Это также поможет вам исправить какие-то ошибки в вашем коде. Мы будем использовать ваш демо для проверки вашего решения. **Пользователь вашего демо должен просто запустить ячейки, вы не должны ожидать от пользователя чего-то большего.**

> [!IMPORTANT]
> Ваш демо ноутбук должен запускаться в **Google Colab** без проблем.

---

## Отчет

В дополнение к стандартным требованиям отчета, проведите следующий анализ качества вашего вокодера:

### 1. Анализ на обучающих данных

Возьмите несколько аудиозаписей из датасета RUSLAN, на котором вы обучали вокодер:
- Извлеките мел-спектрограммы из исходных аудио на тестовом сете
- Сгенерируйте синтезированные версии аудио с помощью вашего вокодера (resynthesize)
- Сравните сгенерированные образцы с оригиналами во **временной** и **частотно-временной** областях (waveform, спектрограммы)
- Ответьте на вопросы:
  - Какие различия вы видите?
  - Можно ли на слух понять, что аудио синтезировано?
  - Можно ли это определить по форме волны или спектрограмме?
  - Какие выводы вы можете сделать?

### 2. Анализ на внешних данных

Возьмите аудиозаписи из тестовых предложений (указанных в разделе MOS):
- Извлеките мел-спектрограммы и сгенерируйте синтезированные версии
- Проведите аналогичное сравнение
- Ответьте на вопросы:
  - Справедливы ли выводы из Inner Analysis?
  - Какие различия вы видите между обучающим и внешним датасетами?
  - Объясните причины различий, если они есть

### 3. Анализ всей TTS системы

Используйте текстовые транскрипции и сгенерируйте аудио с помощью акустической модели (например, из HuggingFace, ESPnet или NVIDIA NeMo) + вашего вокодера:
- **Часть 1**: Возьмите текстовые транскрипции из датасета RUSLAN
- **Часть 2**: Возьмите текстовые транскрипции из внешнего датасета (тестовые предложения)
- Сравните сгенерированные аудио с оригиналами
- Ответьте на вопросы:
  - Какие новые артефакты появились по сравнению с resynthesize режимом?
  - Можете ли вы различить влияние акустической модели и вокодера на качество?
  - Какие ограничения TTS систем вы обнаружили?

**Финальные выводы**: Добавьте общие наблюдения о качестве синтезированных образцов, легкости их обнаружения и ограничениях вашей системы.

> [!TIP]
> Для улучшения оценки рекомендуется вычислить статистику на достаточно большом наборе образцов (10-20 файлов), чтобы подтвердить ваши выводы количественно.

> [!NOTE]
> Анализ на основе 1-2 примеров будет оценен низко. Это исследовательское задание, требующее глубокого анализа.

---

## Полезные ресурсы

**Статьи:**
* [HiFi-GAN (2020)](https://arxiv.org/pdf/2010.05646.pdf)
* [iSTFTNet (2022)](https://arxiv.org/pdf/2203.02395.pdf)
* [Vocos (2023)](https://arxiv.org/pdf/2306.00814.pdf)
* [BigVGAN (2022)](https://arxiv.org/pdf/2206.04658.pdf)
